import React from "react";
import { Typography, Button, Card, CardContent, CardMedia, Container, Grid, AppBar, Toolbar } from "@mui/material";
import { Link } from "react-router-dom";

const HomePage = () => {
  return (
    <div>
       Home sweet Home
    </div>
  );
};

export default HomePage;
